{
  "name": "seat-ase",
  "private": true,
  "description": "Seat Ase? — a Dhaka Tesla Pool MVP",
  "workspaces": [
    "api",
    "shared",
    "web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:api": "npm run dev -w api",
    "test": "npm test -w api",
    "dev:web": "npm run dev -w web"
  }
}
